# Implementation Plans

Generated by the `improve` skill on 2026-06-16, planned against commit `5576703`.
Last reconciled 2026-07-22 against commit `f142763` (plan 012 executed & merged via `#43`,
adding `create_log_indexes.py` and a live UNIQUE `(PLAYER_ID, DATE)` index on all three log
tables; full suite = **52** tests pass. Re-verified all DONE plans hold on HEAD. Refreshed the
four open TODO plans for the new module + test counts and the shifted line anchors: **009**
(21→22 modules, 47→52 tests, `create_log_indexes.py` added to the move/import/smoke lists);
**010** (target file now has 2 pre-existing tests, done-criteria 5→6, anchors shifted);
**011** (target file unchanged — finding holds, suite 47→52); **014** (all
`daily_fantasy_log_upload.py` anchors re-mapped, and Step 3 hardened to re-create plan 012's
now-live unique index that its `if_exists="replace"` rebuild drops).
Previous reconcile 2026-07-19 against `967d88a` (plan 013 merged via `#38`; 47 tests).
Earlier reconcile 2026-06-22 against `c2f810f` (plans 006/007/008 merged; 38 tests).
Plans 010–012 added 2026-06-21 against commit `3844392` (post-006/007 state).
Reviewed 2026-07-21 against `dacd007` (plan 012 `review-plan`): 012 revised to standardize
all three log tables on `(PLAYER_ID, DATE)` — `player_absences` re-keyed from GAME_ID to
DATE — and plan 014 added for the `fantasy_logs` FLOAT-vs-INTEGER ID-typing fix it surfaced.
Execute in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when done.

These plans were produced by a senior-advisor pass that is **read-only on source code**.
Each plan is self-contained — an executor needs only the plan file and the repo.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status | Issue |
|------|-------|----------|--------|------------|--------|-------|
| 001  | Re-encode `requirements.txt` as UTF-8 | P1 | S | — | DONE (re-verified 2026-07-19 @ `967d88a`: ASCII) | [#4](https://github.com/JonnyRank/bigdataball-data/issues/4) |
| 002  | Establish pytest verification baseline + integration harness | P1 | M | 001 (recommended) | DONE (re-verified 2026-07-19: 47 tests pass) | [#5](https://github.com/JonnyRank/bigdataball-data/issues/5) |
| 003  | Fix duplicate-log inserts across files in one run | P1 | S | 002 | DONE (re-verified 2026-07-19: regression test green in full suite) | [#6](https://github.com/JonnyRank/bigdataball-data/issues/6) |
| 004  | Preserve regular-season unmatched-players worklist | P1 | S | 002 (003 recommended) | DONE (re-verified 2026-07-19: `test_orchestrator_warnings` green) | [#7](https://github.com/JonnyRank/bigdataball-data/issues/7) |
| 005  | Centralize data-path resolution into a `paths` module | P2 | M | 002 | DONE (merged `#24`; re-verified 2026-07-19 @ `967d88a`: `paths.py` present, 2 `test_paths` tests pass) | [#8](https://github.com/JonnyRank/bigdataball-data/issues/8) |
| 006  | Extract triplicated DraftKings load + fuzzy-match logic | P2 | M | 002 (005 recommended) | DONE (merged `#26`; re-verified 2026-07-19 @ `967d88a`: `dk_matching.py` present, 8 `test_dk_matching` tests pass) | [#9](https://github.com/JonnyRank/bigdataball-data/issues/9) |
| 007  | Centralize hardcoded season filters | P3 | S | 002 (006 recommended) | DONE (merged `#27`; re-verified 2026-07-19 @ `967d88a`: `seasons.py` present, 3 `test_seasons` tests pass) | [#10](https://github.com/JonnyRank/bigdataball-data/issues/10) |
| 008  | Seed the `map_teams` table for fresh DBs | P2 | M | 005 (DONE) | DONE (merged `#34`; re-verified 2026-07-19 @ `967d88a`: `seed_map_teams.py` present, 9 `test_seed_map_teams` tests pass) | [#11](https://github.com/JonnyRank/bigdataball-data/issues/11) |
| 009  | Convert flat layout to a `src/bigdataball/` package | P2 | M | none (rebases paths of 010–011/014) | TODO (refreshed 2026-07-22 @ `f142763`: plan 012 added `create_log_indexes.py` (`import paths`, no `__file__` fallback) → **22** modules to move; `__file__`-fallback inventory unchanged at 2; test count **52**; new tests need no import changes) | [#32](https://github.com/JonnyRank/bigdataball-data/issues/32) |
| 010  | Expand fantasy log upload test suite | P2 | M | none (fixture + helpers exist) | TODO (re-verified 2026-07-22 @ `f142763`: finding holds — 4 target behaviors still untested; `test_daily_fantasy_log_upload.py` now has **2** pre-existing tests (plan 012 added the index test), done-criteria 5→6, `daily_fantasy_log_upload.py` anchors re-mapped in plan) | [#28](https://github.com/JonnyRank/bigdataball-data/issues/28) |
| 011  | Add tests for `create_summary_tables.py` | P2 | L | none | TODO (re-verified 2026-07-22 @ `f142763`: `create_summary_tables.py` unchanged, still zero tests; conftest fixture line refs (33/84) still hold; suite 47→52) | [#29](https://github.com/JonnyRank/bigdataball-data/issues/29) |
| 012  | Add DB-level UNIQUE constraint to log tables | P1 | M | none (run `check_ingest_duplicates --remove` first on live DB) | DONE (executed 2026-07-21 via sonnet executor, reviewer-approved; commit `221c0f4` fast-forwarded onto this branch, PR [#43](https://github.com/JonnyRank/bigdataball-data/pull/43) → main. All three tables indexed on **(PLAYER_ID, DATE)**; absence dedup + box-score filter re-keyed GAME_ID→DATE. Full suite 52 passed (47+5 new — follow-up commit `085893a` from the PR review added a direct `IntegrityError` enforcement test, normalized `player_logs.DATE` in `_load_box_score_keys`, and guarded the orchestrator's Step 2 `initialize_database()` call). **Deploy gate cleared 2026-07-21**: live-DB `check_ingest_duplicates.py` preflight run — zero duplicates in all three log tables, so the first `CREATE UNIQUE INDEX` will apply cleanly) | [#30](https://github.com/JonnyRank/bigdataball-data/issues/30) |
| 013  | Ingest DNP-DND-NWT sheet into `player_absences` | P1 | M | 002/003 | DONE (2026-07-18 via `#38`; GAME_ID stored as INTEGER — documented deviation; post-merge correction renamed columns to `DATE`/`PLAYER` per repo convention. Verified 2026-07-19: wiring present in `daily_player_upload.py`, 9 `test_absence_ingestion` tests green. **Note 2026-07-21**: its GAME_ID absence dedup key is superseded by plan 012's re-key to (PLAYER_ID, DATE)) | — |
| 014  | Normalize `fantasy_logs.PLAYER_ID`/`GAME_ID` to INTEGER | P2 | M | none (012 now DONE) | TODO (refreshed 2026-07-22 @ `f142763`: finding holds — `fantasy_logs` IDs still FLOAT/REAL vs INTEGER elsewhere. Plan 012 now DONE: all `daily_fantasy_log_upload.py` anchors re-mapped, and Step 3's `if_exists="replace"` rebuild must re-create plan 012's now-live UNIQUE `idx_fantasy_logs_player_date` — spelled out in the plan) | — |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Recommended sequencing

1. **001** first — trivial, independent, unblocks `pip install` for everyone else.
2. **002** next — the verification baseline. It introduces the `BIGDATABALL_DATA_DIR`
   env seam (in the two upload scripts) and the pytest harness that 003 and 004 reuse.
3. **003** and **004** — the two data-integrity bug fixes, each with a regression test on
   the 002 harness. 003 and 004 both touch `daily_fantasy_log_upload.py` in different
   sections; do 003 before 004 to avoid overlapping edits, though they're independent.
4. **005** — generalizes the path seam from 002 to all eight scripts. Do after the bug
   fixes so their tests are already green and act as a safety net for the refactor.
5. **006** then **007** — refactors of the three export scripts. 006 extracts the match
   logic; 007 centralizes season filters. Same files, so sequence them.
6. **008** — independent DX improvement; benefits from 005's `paths.py` but can stand
   alone. Schedule any time after 005.

## Dependency notes

- **003, 004 require 002** because their regression tests use the `player_upload` /
  reload fixture and the `BIGDATABALL_DATA_DIR` seam that 002 introduces.
- **005 requires 002** and *supersedes* the inline env-var branch 002 added to the two
  upload scripts (it moves that logic into `paths.resolve_base_data_path()`). The 002–004
  tests act as the regression guard that the refactor preserved reload semantics.
- **006/007 recommended after 005** only to avoid edit collisions in the export scripts;
  no hard logic dependency.
- **008 recommended after 005** so it can call `paths.resolve_base_data_path()`; it has an
  inline fallback if 005 isn't done yet.
- **009 (src layout) is independent but rebases everything else.** It now moves all 22
  modules into `src/bigdataball/` (006/007/008 added `dk_matching`, `seasons`, and
  `seed_map_teams`; 013 added `absence_ingestion`, `backfill_player_absences`, and
  `patch_absence_column_names`; 012 added `create_log_indexes`), so after it lands the
  in-scope file paths in plans 010/011/014 change from `<module>.py` to
  `src/bigdataball/<module>.py` (logic and line numbers unchanged).
  Two orderings work: (a) do 010/011/014 first, then 009 last — no rebasing needed (this is
  why the downstream plans' drift checks and in-scope lists cite root-level `<module>.py`: they
  are written against the recommended default state, before 009 lands); or (b) do 009 now (as
  requested), in which case prepend `src/bigdataball/` to every module path when executing the
  others — this includes the `git diff … --` **drift-check command** at the top of plans
  010/011/014 and their in-scope file lists (e.g. `daily_fantasy_log_upload.py` →
  `src/bigdataball/daily_fantasy_log_upload.py`). Line numbers and logic are unchanged either
  way. Do **not** pre-rewrite the downstream plans to `src/` paths — that would break ordering
  (a), the default.
  The `__file__`-based `Data/` fallback still lives in exactly **two** files — `paths.py` (the
  live one, from plan 005) and `seed_map_teams.py` (a dead-after-conversion fallback in its
  `except` branch) — so Step 3 of plan 009 deepens both. `create_log_indexes.py` delegates
  path resolution to `paths` and adds no `__file__` fallback. Plan 009 was refreshed again at
  reconcile 2026-07-22 for plan 012's new module (22 modules, 52 tests).

## Findings considered and rejected (so they aren't re-audited)

- **SQL string interpolation in IN-lists** (export scripts, `run_db_patch.py`): player
  names are DB-sourced and single-quote-escaped (`.replace("'", "''")`) before
  interpolation; table/column names in the patch script are hardcoded constants. Not
  meaningfully exploitable here. Plan 006 keeps the existing escaping via `to_sql_in_list`.
- **Performance**: data volumes are tiny (hundreds of players, low-thousands of logs).
  Full-table pandas loads and O(n·m) fuzzy matching are fine; no real wins. Not worth doing.
- **Credential hygiene**: `.env`, `token.json`, `client_secrets.json` are all git-ignored
  and nothing sensitive is committed. Clean — no action.
- **Duplicate `os.makedirs(PROCESSED_FOLDER, exist_ok=True)` lines / dead commented code**
  at the end of `daily_player_upload.py`: trivial; fold into another change if convenient,
  not worth a dedicated plan.
- **Misleading orchestrator filename** (`daily_fantasy_log_upload.py` is the orchestrator):
  documented in `CLAUDE.md`; renaming would break the documented invocation. Not worth it.

## New findings considered and rejected (plans 010–012 audit)

- **Structured logging (`logging` module)** — All scripts use `print()`. Replacing with
  `logging` would improve post-run diagnostics for headless scheduled runs (CONCERNS.md:12).
  Effort L (all modules), low risk, but low urgency since the email notification already
  delivers a summary. Deferred — not worth a plan until the scheduling moves off Windows
  Task Scheduler.
- **CSV export accumulation** — `csv_exports/` grows by 2+ files per day with no cleanup.
  Minor operational concern; the files are small. Not worth a plan (trivial manual cleanup
  or a simple `glob.glob` + `os.remove` one-liner if needed).
- **`L30_DK_POINTS_sum`/`L30_MINUTES_sum` extra columns in `fantasy_averages`** — The
  rename map in `create_summary_tables.py` doesn't rename these intermediate aggregation
  columns; they persist in the table. Cosmetic; downstream queries ignore them. Not worth
  a dedicated plan.
- **Direction: migrate daily trigger to GitHub Actions** — The Windows Task Scheduler
  dependency (CONCERNS.md:15) could be replaced with a scheduled GH Actions workflow
  (`on: schedule: cron`). Deferred: requires secrets management for the Google Drive
  token and Gmail credentials, which is a meaningful scope change.

## What was NOT audited

- Runtime behavior against real Google Drive / Gmail / DraftKings inputs (no credentials,
  no sample data, DB is git-ignored). Findings are from static reading of the code.
- `.github/` agent/skill definitions and the Claude GitHub Action workflows (tooling, not
  pipeline code).
- Exact NBA team raw-name strings and player-mapping completeness (data questions that
  require the live DB) — plan 008 handles `map_teams` defensively rather than assuming them.
